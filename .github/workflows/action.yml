name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - run: pwd
      - run: ls -a
      - run: pip install .
      - run: pip list 
      - run: pytest -v
