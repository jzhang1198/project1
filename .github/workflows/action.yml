name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: cd ..
      - run: pwd
      - run: ls -a
      - run: pip install . 
      - run: pytest -v
